# Linux的权限系统基于 "用户-组-其他" 三组 和 "读-写-执行" 三位

# 基本概念:
文件拥有者(u) : 文件的主人
组(g) : 文件所属的用户组
其他人(o) : 既不是拥有者，也不属于组的用户

读(r) : 查看文件内容/列出目录内容
写(w) : 修改文件/在目录中创建或删除文件
执行(x) : 运行文件 (如果是脚本或程序) /进入目录 (cd)

# 权限表示
符号方式: -rwxr-xr-x
第1个字符：- (普通文件), d(目录), l(符号链接)
第2-4字符: 拥有者权限 (rwx)，rwx的顺序必须固定
第5-7字符: 组权限 (r-x)，如果某个权限没有，就显示 - (这里缺少w）
第8-10字符: 其他人权限 (r-x)

数字方式(八进制)
r=4 , w=2 , x=1
7 = 4+2+1 = rwx
6 = 4+2 = rw-
5 = 4+1 = r-x
4 = r--
例子：755 = 拥有者rwx , 组r-x , 其他人r-x

# 常用命令
chmod 755 script.sh          # 设置脚本权限为755
chmod u+x script.sh          # 为拥有者添加执行权限
chmod +x script.sh           # 相当于 chmod a+x , a=all(u+g+o)
chmod user:group script.sh   # 更改文件拥有者和组

# 用户管理 (sudo,su)
用户 : 对每个用户有唯一的用户名和UID(用户ID)，root的UID=0
组 : 用户组的集合，GID(组ID)，一个用户可以属于多个组



# TCP/IP 四层模型
应用层(直接为用户或程序提高网络服务）：
典型协议：HTTP/HTTPS(网页)、DNS(域名解析)、FTP(文件传输)、SSH(远程登入)、SMTP(邮件发送)

传输层(在两台主机之间建立端到端通信，控制数据传输方式):
核心协议:TCP(可靠传输，有确认机制)、UDP(不保证可靠，速度快)
关键概念：端口号(0-65535)、三次握手、四次挥手、重传机制、流量控制

网络层(负责数据在不同网络之间的转发)
核心协议:IP(IP地址)、ICMP(ping)、ARP(地址解析)
关键点:IP地址、子网、路由转发

网络接口层(处理本地网络中的实际数据传输)
包括:以太网、MAC地址、网卡驱动

# 举例
(访问一个网站)：
1. 浏览器准备连接信息 (应用层)
2. TCP 构造握手报文 (传输层)
3. 每个 TCP 报文都被 IP 封装 (网络层)
4. IP 再被以太网封装发送 (网络接口层)
5. 对方收到后反向拆包(解封装)

# 三次握手完成后,HTTP 也数据按相同方式封装发送

# 和 OSI 七层模型的关系
TCP/IP     OSI对应层
应用层    = 应用+表示+会话
传输层    = 传输层
网络层    = 网络层
网络接口层 = 数据链路+物理

# TCP/IP 是工程实际使用模型，OSI是教学抽象模型

# 补充
Ping 成功只能代表网络层可达 (ICMP正常) , 并不代表应用层可用 (端口443被防火墙阻断，DNS解析错误，TCP三次握手失败，服务器应用奔溃)

# TCP (三次握手，四次挥手): 
建立连接 -> 三次握手 (确认双方都具备收发能力，并同步初始化序列号)
第一次: 客户端 -> 服务器 (SYN : 表示我要建立连接，发送自己的初始序列号 seq=x)
第二次: 服务器 -> 客户端 (ACK + SYN : ACK=x+1(确认客户端的SYN),同时发送自己的序列号 seq=y)
第三次: 客户端 -> 服务器 (ACK : ACK=y+1,确认服务器的SYN)
(如果只有两次，服务器无法确认客户端是否真收到自己的SYN,以及是否存在历史连接的"陈旧SYN"导致误建立)

数据传输阶段:
(持续收发: 每一段数据都有序列号，每个字节都有编码。双方必须ACK确认，若超时未确认，会重传。有流量控制和拥塞控制)

断开连接 -> 四次挥手:
(TCP 是"全双工" , 双方的发送通道独立关闭，所以需要两次 FIN）
谁先发起挥手并不固定（可以是客户端，也可以是服务器）)
第一次: A -> B (FIN : A 表示我不再发送数据，进入FIN_WAIT_1)
第二次: B -> A (ACK : B 表示确认收到FIN，A 进入FIN_WAIT_2 ，B 进入CLOSE_WAIT 。此时 A 不能再发数据，但仍可以接收 B 的数据)
第三次: B -> A (FIN : B 处理完剩余数据后，也关闭发送通道，进入LAST_ACK)
第四次: A -> B (ACK : A 确认，A 进入 TIME_WAIT 状态，通常2MSL,防止最后一个 ACK 丢失，防止旧连接的数据干扰新连接)

# 建立连接常见状态 (LISTEN 、SYN_SENT 、SYN_RECEIVED 、ESTABLISHED)
# 断开常见状态 (FIN_WAIT_1 、FIN_WAIT_2 、CLOSE_WAIT 、LAST_ACK 、TIME_WAUT)

# UDP (无连接，不可靠，面向报文，延迟比可靠更重要)
头部结构(8字节)：源端口，目的端口，长度，效验和


# TLS（基于TCP，保证了机密性，完整性，身份认证）
第一步：Client Hello（客户端发支持的TLS版本，支持的加密算法，随机数，SNI（访问的域名））
第二步：Server Hello（服务器返回选择的加密算法，服务器随机数，密钥交换参数）
第三步：服务器继续发送：数字证书（含公钥），CA签名链。
       客户端会验证证书合法性，是否由可信CA签发，是否匹配域名..继而提取公钥，用公钥做密钥交换，生成对称密钥。后续用密钥对称加密通信
在 TLSv1.3 中，Server Hello 后很快就 APPlication Date（只需要一个往返 1RTT，支持 0-RTT）之后抓包只能看到 Encrypted Application Date ，无法看到 HTTP 内容（HTTP 在 TLS 之上，而 TLS 加密了所有 payload）

# TLS证书链（从下往上信任，用于证明公钥属于这个网站，并非用于加密数据本身）：系统信任根CA -> 根CA信任中级CA -> 中级CA信任网站
网站证书 (这是服务器发的证书，包含域名(Suject)，谁签发(Issuer)，公钥(Public Key)，有效期(Valid From/To)，签名(Signature)) 
中级 CA 证书 (由根CA签发，用来签网站证书，可以有多层（避免根证书频繁使用）)
根 CA 证书（自签名，存在于系统中，操作系统/浏览器预装）

通过层层签名，把网站公钥最终关联到系统内置的根信任

验证步骤：
1.网站证书是否由中级CA签名
2.中级CA是否由某个根CA签名
3.检查这个根CA是否在系统信任列表
4.检查域名是否匹配
5.检查证书是否过期

# TLS（基于TCP，保证了机密性，完整性，身份认证）

第一步：Client Hello（客户端发支持的TLS版本，支持的加密算法，随机数，SNI（访问的域名））
第二步：Server Hello（服务器返回选择的加密算法，服务器随机数，密钥交换参数）
第三步：服务器继续发送：数字证书（含公钥），CA签名链。
       客户端会验证证书合法性，是否由可信CA签发，是否匹配域名..
       双方进行 ECDHE 密钥交换（非对称数学协商）生成对称会话密钥     # 老版本则是提取公钥，用公钥做密钥交换，生成对称密钥
       后续全部用对称加密传输数据

# TLS证书链（从下往上信任，用于证明公钥属于这个网站，并非用于加密数据本身）：
系统信任根CA -> 根CA信任中级CA -> 中级CA信任网站（ 通过层层签名，把网站公钥最终关联到系统内置的根信任 ）

网站证书 (这是服务器发的证书，包含域名(Suject)，谁签发(Issuer)，公钥(Public Key)，有效期(Valid From/To)，签名(Signature，由CA用私钥签发)) 
中级 CA 证书 (由根CA签名，用来签网站证书，可以有多层（避免根证书频繁使用）)
根 CA 证书（自签名，存在于系统中，操作系统/浏览器预装，不需要发送）

验证步骤：
1.客户端用CA公钥验证，确认网站证书是否由中级CA签名 (非对称加密运用)
2.中级CA是否由某个根CA签名
3.检查这个根CA是否在系统信任列表
4.检查域名是否匹配
5.检查证书是否过期

# TLS 1.3 变化
1.3 不再支持 RSA密钥传输，必须使用 ECDHE(不直接加密密钥，双方根据数学运算生成同一个共享密钥，过程中不会传输密钥) 这是非对称加密机制，但不是加密传输密钥，而是协商加密
Server Hello 后极快就进入 APPlication Date（只需要一个往返 1RTT，支持 0-RTT）

之后抓包只能看到 Encrypted Application Date 无法看到 HTTP 内容（HTTP 在 TLS 之上，而 TLS 加密了所有 payload）



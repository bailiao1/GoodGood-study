# IP (Internet Protocol)
(IP地址是网络中设备的逻辑地址，用于在网络层标识"哪一台机器")
IP 解决的是数据该送往哪一台主机，不关心端口，不关心应用，只负责主机级定位
IPv4 ( 示例: 192.168.1.10 )
IPv6 ( 示例: 2001:db8::1 )

# 端口 (端口是传输层(TCP/UDP)的概念)
IP 只能定位到一台机器，但一台机器上可能同时运行: Web服务(80/443) , SSH(22) , 数据库(3306) ， 自己写的服务(8000)
端口用于标识这台机器上的哪个程序
范围：0-65535 （0-1023：系统端口），（1024-49151: 注册端口），(49152-65535:临时端口,客户端常用)

# IP + 端口 = 一个完整的通信目标
示例：93.184.216.34:443 (这台IP的主机上面的443端口（通常是HTTPS服务）)

# DNS (Domain Name System 域名系统）
DNS 不是一台机器，而是一个分布式、层级化的系统
作用为将域名转为IP ：A记录(IPv4) , AAAA记录(IPv6)
此外，还包括：
CNAME记录（把一个域名别名指向另一个域名（规范名））, MX记录(邮件服务器) , TXT记录(验证域名所有权，SPF反垃圾邮件等)

示例: www.google.com -> 142.250.190.78 
1：浏览器内本地查询
浏览器先向本地缓存询问（我最近查过这个地址吗？有就用）

2：系统本机查询
系统问本地hosts文件 (Windows/Linux有手写过这个域名的固定ip吗？)

3：请求路由器
电脑问询路由器（本地DNS）进行递归查询

4: 路由器问自己的“上游 DNS 服务器”
若路由器也没有缓存，路由器就转发询问上游DNS ( 通常是ISP自动分配，替路由器问询全球DNS体系 )

5：ISP 的 DNS 开始真正的递归查询
先问根域名服务器：".com归谁管？"
指向 .com 顶级域名服务器，得到 google.com 的权威服务器是 ns1.google.com
再去问 google 自己的DNS服务器："www 的IP 多少？" 拿到 142.250.190.78 (真正的权威答案在域名持有者自己的服务器里)

6: IP 沿原路返回
ISP 的 DNS → 路由器 → 电脑 → 浏览器

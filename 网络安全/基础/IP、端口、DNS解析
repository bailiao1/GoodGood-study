# IP (Internet Protocol)
(IP地址是网络中设备的逻辑地址，用于在网络层标识"哪一台机器")
IP 解决的是数据该送往哪一台主机，不关心端口，不关心应用，只负责主机级定位
IPv4 ( 示例: 192.168.1.10 )
IPv6 ( 示例: 2001:db8::1 )

# 端口 (端口是传输层(TCP/UDP)的概念)
IP 只能定位到一台机器，但一台机器上可能同时运行: Web服务(80/443) , SSH(22) , 数据库(3306) ， 自己写的服务(8000)
端口用于标识这台机器上的哪个程序
范围：0-65535 （0-1023：系统端口），（1024-49151: 注册端口），(49152-65535:临时端口,客户端常用)

# IP + 端口 = 一个完整的通信目标
示例：93.184.216.34:443 (这台IP的主机上面的443端口（通常是HTTPS服务）)

# DNS (Domain Name System 域名系统）
DNS 不是一台机器，而是一个分布式、层级化的系统
作用为将域名转为IP ：A记录(IPv4) , AAAA记录(IPv6)

示例: www.google.com -> 142.250.190.78 
1：浏览器内本地查询
浏览器先向本地缓存询问（我最近查过这个地址吗？有就用）

2：系统本机查询
系统问本地hosts文件 (Windows/Linux有手写过这个域名的固定ip吗？)

3：请求路由器
电脑问询路由器（本地DNS）进行递归查询

4: 路由器问自己的“上游 DNS 服务器”
若路由器也没有缓存，路由器就转发询问上游DNS ( 通常是ISP自动分配，替路由器问询全球DNS体系 )

5：ISP 的 DNS 开始真正的递归查询
先问根域名服务器：".com归谁管？"
指向 .com 顶级域名服务器，得到 google.com 的权威服务器是 ns1.google.com
再去问 google 自己的DNS服务器："www 的IP 多少？" 拿到 142.250.190.78 (真正的权威答案在域名持有者自己的服务器里)

6: IP 沿原路返回
ISP 的 DNS → 路由器 → 电脑 → 浏览器

此外，DNS还包括：
CNAME记录（把一个域名别名指向另一个域名（规范名））, MX记录(邮件服务器) , NX(权威DNS服务器) , TXT记录(验证域名所有权，SPF反垃圾邮件等)

# 补充
# TTL(Time of Live)：缓存时间
 例子: TTL = 600 秒  # 表示DNS结果可以缓存10分钟，减少查询压力，但IP更变不会立即生效

# 递归解析 与 迭代解析
递归解析(平时使用的)：DNS服务器替你一路问到底，最终得到答案
迭代解析: DNS服务器不会帮你问完，而是告诉你去问.com服务器，然后你需要自己去问

# DNS污染，劫持原理
DNS 本质是 UDP 协议(默认53端口)，没有加密
因此可以：被篡该返回假IP，被中间人劫持
解决方法：DOH-走HTTPS（443端口,加密传输），DOT-走TLS (853端口)

# CDN加速
DNS 解析返回的 IP 并不是一个固定 IP ，而是离你最近的服务器
DNS 根据来源 IP ，地理位置，负载情况，返回不同 IP
这就是 CDN 智能解析，本质上依赖 DNS

# 常见端口 (本身不是功能，只是操作系统监听某个程序的入口编号)
22 - SSH ( 协议: SSH(Secure Shell)  用于原远程登陆服务器，基于TCP，加密通信，支持密钥登陆，可做端口转发。暴露公网容易被暴力破解 )
80 - HTTP ( 协议: HTTP 基于TCP，用于网页明文传输。无加密，公网中几乎不再用于正式业务 )
443 - HTTPS ( 协议: HTTPS (HTTP + TLS)，基于TCP，用于加密网页访问。TLS加密，证书验证，防止中间人攻击。现在的公网主流协议 )
853 - DNS over TLS ( 协议:DNS ver TLS (DoT)，基于TCP，用于加密DNS查询，TLS加密，证书验证，防止被监听/篡改等中间人攻击。加密DNS标准端口 )
3306 - MySQL ( 协议: MySQL数据库服务，用于数据库服务器监听端口，应用程序连接数据库。极高风险端口(如果暴露公网)：被暴力破解，被扫库，数据被拖走 .. )
3389 - RDP ( 协议: RDP (Remote Desktop Protocol)，用于 Windows 远程桌面，远程控制Windows服务器，图形化桌面访问。风险：漏洞利用，勒索攻击常见入口)

# 常见攻击者流程
1: 扫端口
2: 判断服务类型
3: 查漏洞
4: 暴力破解或利用

# IP (Internet Protocol)
(IP地址是网络中设备的逻辑地址，用于在网络层标识"哪一台机器")
IP 解决的是数据该送往哪一台主机，不关心端口，不关心应用，只负责主机级定位
IPv4 ( 示例: 192.168.1.10 )
IPv6 ( 示例: 2001:db8::1 )

# 端口 (端口是传输层(TCP/UDP)的概念)
IP 只能定位到一台机器，但一台机器上可能同时运行: Web服务(80/443) , SSH(22) , 数据库(3306) ， 自己写的服务(8000)
端口用于标识这台机器上的哪个程序
范围：0-65535 （0-1023：系统端口），（1024-49151: 注册端口），(49152-65535:临时端口,客户端常用)

# IP + 端口 = 一个完整的通信目标
示例：93.184.216.34:443 (这台IP的主机上面的443端口（通常是HTTPS服务）)

# DNS (Domain Name System 域名系统 ：作用是把域名翻译为IP地址）

示例: www.google.com -> 142.250.190.78
第一步：
